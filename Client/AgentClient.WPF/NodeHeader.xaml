<UserControl x:Class="SuperSocket.Management.AgentClient.NodeHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:ia="http://schemas.microsoft.com/expression/2010/interactions"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal" Background="{StaticResource HoverGradient}" HorizontalAlignment="Stretch" >
            <TextBlock Style="{StaticResource NodeTitle}"
                                Text="{Binding Name}" Grid.Row="0"></TextBlock>
            <TextBlock Style="{StaticResource NodeStatus}"
                                Text="{Binding State, StringFormat=' [\{0\}]'}" Grid.Row="0"></TextBlock>
        </StackPanel>
        <Grid DataContext="{Binding GlobalInfo}" DataContextChanged="Grid_DataContextChanged"
              Style="{StaticResource GlobalInfoGrid}">
        </Grid>
    </StackPanel>
</UserControl>
